export type Theme = {
  bg: string;            // warna latar layar
  card: string;          // panel/kartu/input
  text: string;          // teks utama
  textDim: string;       // teks sekunder
  primary: string;       // warna aksen (chip aktif, dsb.)
  primarySoft: string;   // aksen versi soft (background chip aktif)
  danger: string;        // teks/ikon berbahaya
  dangerSoft: string;    // background tombol berbahaya
  border: string;        // garis/border halus
};
